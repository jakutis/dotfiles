#!/usr/bin/env bash

TYPE="$1"
COMMAND="$2"

CARD="$(cat "$HOME/.varia/soundcard")"

CURVOL=$(amixer -c $CARD cget name="Master Playback Volume"|grep ": values"|sed 's/  : values=//ig'|sed 's/,.*//ig' 2>/dev/null)
CURVOL_PULSE=$(pacmd dump-volumes|perl -n -e'/Sink (\d+):.*, real = (.*), soft = / && print $2')
MAXVOL=$(amixer -c $CARD sget "Master"|grep Limits|sed 's/  Limits: Playback 0 - //ig')
if [ "$TYPE" = "alsa" ]
then
    STEP=$(($MAXVOL / 20))
    if [ "$COMMAND" == "increase" ]
    then
        if [ "$CURVOL" -lt "$MAXVOL" ]
        then
            CURVOL=$(($CURVOL + $STEP))
        fi
    elif [ "$COMMAND" == "decrease" ]
    then
        if [ "$CURVOL" -gt "0" ]
        then
            CURVOL=$(($CURVOL - $STEP))
        fi
    else
        echo "Error: unknown command $COMMAND"
        exit 1
    fi

    amixer -c $CARD Master $CURVOL
elif [ "$TYPE" = "pulse" ]
then
    if [ "$COMMAND" = "decrease" ]
    then
        pactl set-sink-mute $CARD 0
        pactl set-sink-volume $CARD -- -5%
    elif [ "$COMMAND" = "increase" ]
    then
        pactl set-sink-mute $CARD 0
        pactl set-sink-volume $CARD -- +5%
    else
        echo "Error: unknown command $COMMAND"
        exit 1
    fi
else
    echo "Error: unknown sound system type $TYPE"
    exit 1
fi
CURVOL=$(amixer -c $CARD name="Master Playback Volume"|grep ": values"|sed 's/  : values=//ig'|sed 's/,.*//ig' 2>/dev/null)
CURVOL_PULSE=$(pacmd dump-volumes|perl -n -e'/Sink (\d+):.*, real = (.*), soft = / && print $2')
#awesome-client <<< "myvolumewidget.text='<span color=\"blue\"> $((CURVOL * 100 / $MAXVOL))% (${CURVOL_PULSE}) </span>'"
