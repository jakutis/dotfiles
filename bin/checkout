#!/usr/bin/env bash

if [ -n "$2" ]
then
  BRANCH="$2"
else
  BRANCH="master"
fi

(cd "$HOME/repos/git/$1" && [ "$(git branch --show-current)" = "master" ] && git pull --rebase) && cp -r "$HOME/repos/git/$1" . && cd "$1" && git clean -ffxd . && git checkout "$BRANCH" && ide
