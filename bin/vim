#!/usr/bin/env bash

. lang ensurebins rust && . lang ensurebins node && . lang ensurebins python && . lang ensurebins ruby && . lang ensurebins brew && [ "$(file --dereference --brief --mime-type "$(which vim)")" != "text/x-shellscript" ] && vim "$@"