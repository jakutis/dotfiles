#!/usr/bin/env bash

. lang ensurebins rust && . lang ensurebins node && . lang ensurebins python && . lang ensurebins ruby && \
  export PATH="$HOME/.bin:$HOME/bin:$PATH" && "$HOME/opt/vim/bin/vim" "$@"