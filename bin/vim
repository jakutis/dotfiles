#!/usr/bin/env bash

. lang ensurebins rust && . lang ensurebins node && . lang ensurebins python && . lang ensurebins ruby && . lang ensurebins brew || exit 1

VIM="$(which vim)"
export PATH="$HOME/.bin:$HOME/bin:$PATH"
"$VIM" "$@"