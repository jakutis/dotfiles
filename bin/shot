#!/usr/bin/env bash

FN="$HOME/Downloads/screenshot-$(date "+%F-%s").png"
PAUSE="$1"
ARGS=""
if [ -n "$PAUSE" ]
then
  sleep "$PAUSE"
  ARGS="$ARGS -window root"
fi
ARGS="$ARGS $FN"
import $ARGS && gimp "$FN"