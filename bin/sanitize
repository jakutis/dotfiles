#!/usr/bin/env bash

function sanitize {
  (tr -c '[:graph:]' ' '| sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//';echo)|tr + ' '|tr - ' '|tr . ' '|inline-detox
}

if [ -n "$1" ]
then
  echo "$1" | sanitize
else
  sanitize
fi