#!/usr/bin/env bash

for SRV in ejabberd pcscd uuidd ModemManager mysql apache3 dnsmasq smbd haproxy minissdpd minidlna avahi-daemon nmbd ocserv epmd iio-sensor-proxy unattended-upgrades packagekit
do
  systemctl disable $SRV && systemctl stop $SRV
done
