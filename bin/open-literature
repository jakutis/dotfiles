#!/usr/bin/env bash

cat "$1" | grep -v '^#' | (
    PDFS=()
    while read PDF
    do
        PDFS=("${PDFS[@]}" "$PDF.pdf")
    done
    cd "$HOME/etc/storage/books"
    "$HOME/opt/qpdfview/usr/bin/qpdfview" "${PDFS[@]}"
)
