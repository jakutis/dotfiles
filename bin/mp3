#!/usr/bin/env bash

INPUT="$1"

EXT="${INPUT##*.}"

if [ "$EXT" = "mp3" ]
then
  exit 0
fi

OUTPUT="$(basename "$INPUT" "$EXT")mp3"

if [ -e "$OUTPUT" ]
then
  exit 0
fi

ffmpeg -i "$INPUT" -f wav - | lame -V0 - "$OUTPUT"