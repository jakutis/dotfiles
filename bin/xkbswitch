#!/usr/bin/env bash

if [ "$(cat "$HOME/.xkblayout")" = "us" ]
then
    #setxkbmap -layout lt -variant us -symbols "pc+lt(us)+inet(evdev)"
    setxkbmap -layout lt
    echo lt > "$HOME/.xkblayout"
else
    setxkbmap -layout us
    echo us > "$HOME/.xkblayout"
fi

xmodmap "$HOME/.Xmodmap"
