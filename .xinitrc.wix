#!/usr/bin/env bash

xhost localhost
xrdb -merge "$HOME/.Xresources"
xbindkeys
update-xinput
google-drive-ocamlfuse -skiptrash -headless -m /mnt/google
start-minidlna
xscreensaver -no-splash &
autocutsel -buttonup -fork
autocutsel -selection PRIMARY -buttonup -fork
awesome
