#!/usr/bin/env bash

xhost localhost
xrdb -merge "$HOME/.Xresources"
xbindkeys
update-xinput
start-pulseaudio-x11
start-minidlna
xscreensaver -no-splash &
autocutsel -selection PRIMARY -buttonup -fork
autocutsel -selection SECONDARY -buttonup -fork
autocutsel -selection CLIPBOARD -buttonup -fork
awesome
